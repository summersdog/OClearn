1.autoreleasepool自动释放池
2.NSLog与printf使用方式相似，主要优点有：
    1.有调试信息，时间，进程线程相关信息；
    2.会自动换行；
    3.支持OC中的新增数据类型；
  用法：
    1.占位符用法等都一样；
3.NSString，更好用的String，在OC程序中，前缀带有@符号的即为OC风格字符串，类型为NSString；
4.learn

